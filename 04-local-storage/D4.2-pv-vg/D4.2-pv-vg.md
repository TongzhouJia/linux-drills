# KP D4.2 — Physical Volumes & Volume Groups (pvcreate / vgcreate)
**6 tasks · RHEL 10 · server2 as root · assume spare LVM-type partitions on /dev/sdb**

**1.** Create a **physical volume** on `/dev/sdb2`.

**2.** Display all physical volumes on the system and save the output to `/root/pvs.txt`.

**3.** Create a **volume group** named `data_vg` using `/dev/sdb2`.

**4.** Display the details of `data_vg` and save them to `/root/vg.txt`.

**5.** Create a second volume group named `custom_vg` with a **physical extent (PE) size of 8 MiB**.

**6.** Given an extra PV that has been added to a VG, remove that PV **from the VG** and then remove the PV label entirely (clean `vgreduce` + `pvremove`).

---
*Ask "D4.2 答案" when done, or say "继续" for the next batch.*
