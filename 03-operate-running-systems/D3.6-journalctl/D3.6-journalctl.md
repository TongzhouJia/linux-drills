# KP D3.6 — Logs & Journals (journalctl)
**6 tasks · RHEL 10 · server1 as root**

**1.** Show the journal messages from the **current boot** and save the last 50 lines to `/root/journal_boot.txt`.

**2.** Show only **kernel** messages from this boot and save them to `/root/kmsg.txt`.

**3.** Show journal entries for the **sshd** service since **today** and save to `/root/sshd_log.txt`.

**4.** Show all journal entries at priority **`err`** or worse and save to `/root/errs.txt`.

**5.** Show all journal entries from the **last hour** and save to `/root/lasthour.txt`.

**6.** Follow the journal for the `sshd` unit **live** (real-time), and state the exact command used.

---
*Ask "D3.6 答案" when done, or say "继续" for the next batch.*
