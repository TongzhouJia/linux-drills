# KP D3.2 — Rescue Mode / Reset Root Password / Interrupt Boot
**8 tasks · RHEL 10 · ⭐⭐⭐ do these on server2 in a VM (snapshot first)**

**1.** The root password on server2 is unknown. Interrupt the boot at the GRUB menu, break into the system, and **reset the root password to `password`**. Reboot and confirm you can log in.

**2.** When resetting the root password on a system with SELinux **enforcing**, which single step must you run so `/etc/shadow` is relabeled and login still works? Perform it as part of task 1.

**3.** After using the `rd.break` method, the real root filesystem is mounted **read-only** under `/sysroot`. Perform the exact sequence needed to get a writable chroot into the real system (remount + chroot).

**4.** Boot server2 into **emergency** mode from the GRUB menu (one-time kernel edit) and confirm you reach a root shell.

**5.** One-time boot server2 into the **rescue** target by editing the kernel line at GRUB (use the `systemd.unit=` argument). State the exact argument you append and the key you press to boot.

**6.** A bad entry in `/etc/fstab` on server2 prevents normal boot. Boot into rescue mode, comment out the offending line, and return the system to a normal boot.

**7.** From a **running** system, switch into rescue mode using a single `systemctl` command (and know which command gives emergency mode instead).

**8.** Verify the whole reset worked: reboot normally and log in as root with the new password.

---
*Ask "D3.2 答案" when done, or say "继续" for the next batch.*
