# KP D3.5 — Tuning Profiles (tuned-adm)
**5 tasks · RHEL 10 · server1 as root**

**1.** Show the currently **active** tuned profile and save it to `/root/tuned_active.txt`.

**2.** List **all available** tuned profiles and save them to `/root/tuned_list.txt`.

**3.** Switch the active profile to `powersave`.

**4.** Ask tuned to **recommend** a profile for this machine and save the recommendation to `/root/tuned_reco.txt`.

**5.** Create a **merged** custom profile that combines both `powersave` and `virtual-guest`, and make it the active profile. Verify it is active.

---
*Ask "D3.5 答案" when done, or say "继续" for the next batch.*
