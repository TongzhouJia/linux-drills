# KP D3.4 — Process Scheduling (nice / renice)
**5 tasks · RHEL 10 · server1 as root**

**1.** Start `sleep 800` with a **nice value of 10** and confirm its NI value with `ps -o pid,ni,cmd`.

**2.** Change the nice value of that already-running process to **15** using `renice`.

**3.** Start a process with the **highest** possible priority (nice `-20`) and verify it.

**4.** Find the current nice value of a given running `sleep` process and save it to `/root/nice.txt`.

**5.** Take an already-running `sleep` process and lower its priority to the **lowest** (niceness `19`), then verify with `ps -o pid,ni,cmd`.

---
*Ask "D3.4 答案" when done, or say "继续" for the next batch.*
