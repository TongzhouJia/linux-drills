# KP D3.1 — Boot Targets (get/set-default, isolate)
**6 tasks · RHEL 10 · server1 as root**

**1.** Display the current default systemd target and save it to `/root/current_target.txt`.

**2.** Configure the system so it boots into the **multi-user** (text) target by default from now on.

**3.** Switch the **running** system to the multi-user target **right now**, without rebooting.

**4.** Set the default target back to **graphical**.

**5.** List all available targets on the system and save the list to `/root/targets.txt`.

**6.** Determine which systemd target is the equivalent of the old SysV **runlevel 3**, and write its name into `/root/rl3.txt`.

---
*Ask "D3.1 answers" when done, or say "more" for the next batch.*
