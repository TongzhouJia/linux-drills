# KP D6.1 — Schedule Tasks (at & cron)
**6 tasks · RHEL 10 · server1 as root**

**1.** Use `at` to write `This task was easy!` to `/coolfiles/at_job.txt` in **10 minutes** from now.

**2.** List the pending `at` jobs and save them to `/root/atq.txt`, then remove one of them.

**3.** Create a **cron** job (root's) that appends `Wow! I'm going to pass this test!` to `/var/log/messages` **every Tuesday at 3:00 PM**.

**4.** Create a cron job for user **manny** that runs `/home/manny/report.sh` **every day at 06:30**.

**5.** Create a cron job that runs a command **every 5 minutes**.

**6.** Ensure the `atd` and `crond` services are running so the scheduled jobs will actually fire.

---
*Ask "D6.1 answers" when done, or say "more" for the next batch.*
