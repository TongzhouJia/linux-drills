# KP D6.6 — Configure Repositories
**6 tasks · RHEL 10 · server1 as root · repo server = 192.168.55.47**

**1.** Configure a repo pointing to **BaseOS** at `http://192.168.55.47/repo/BaseOS/` with `gpgcheck=0`, and confirm it works.

**2.** Configure an **AppStream** repo at `http://192.168.55.47/repo/AppStream/`.

**3.** Copy the repo file to **server2** using `scp` so it has the same repos.

**4.** List all configured/enabled repos and save the output to `/root/repolist.txt`.

**5.** Add a repo with `dnf config-manager --add-repo <url>`, then **disable** it.

**6.** Build a **local repo** from a directory of `.rpm` files using `createrepo_c`, and point a `.repo` file at it via a `file://` baseurl.

---
*Ask "D6.6 answers" when done, or say "more" for the next batch.*
