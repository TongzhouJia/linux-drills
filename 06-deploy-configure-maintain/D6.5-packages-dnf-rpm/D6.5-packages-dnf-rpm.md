# KP D6.5 — Install & Update Packages (dnf / rpm)
**6 tasks · RHEL 10 · server1 as root**

**1.** Install the `httpd` package.

**2.** Check whether the `bind` package is installed using `rpm` (query).

**3.** Install a package from a **local `.rpm` file** using `rpm` (`-ivh`).

**4.** Find out **which package provides** the file `/usr/sbin/semanage`.

**5.** List all installed packages and save the total **count** to `/root/pkgcount.txt`.

**6.** Install the `star` package from a **specific disabled repo by ID** (e.g. `dnf install -y star --repo F37`), leaving that repo otherwise disabled.

---
*Ask "D6.5 答案" when done, or say "继续" for the next batch.*
