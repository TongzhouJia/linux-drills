# KP D9.6 — SELinux Port Labels
**6 tasks · RHEL 10 · server1 as root · SELinux enforcing**

**1.** Make `sshd` able to listen on **port 9998** by adding the SELinux port label `ssh_port_t` for `tcp/9998`.

**2.** List all SELinux port labels for the **http** service and save them to `/root/http_ports.txt`.

**3.** Allow `httpd` to use the non-standard port **8090** (`http_port_t`).

**4.** **Remove** a custom port label you previously added.

**5.** Verify a port label was added correctly with `semanage port -l`.

**6.** End-to-end: to run a service on a custom port, name the **three layers** you must configure (SELinux port label, firewall, service config) and perform them for one example port.

---
*Ask "D9.6 answers" when done, or say "more" for the next batch.*
