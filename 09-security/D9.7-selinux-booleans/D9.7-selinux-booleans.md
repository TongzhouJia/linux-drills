# KP D9.7 — SELinux Booleans
**6 tasks · RHEL 10 · server1 as root · SELinux enforcing**

**1.** List all SELinux booleans related to `httpd` and save them to `/root/httpd_bools.txt`.

**2.** Allow `httpd` to make outbound **network connections** (`httpd_can_network_connect`) **persistently**.

**3.** Show the current value of a specific boolean (e.g. `httpd_enable_homedirs`).

**4.** Enable a boolean that allows **FTP** access to user home directories, persistently.

**5.** Turn a given boolean **off** persistently.

**6.** Explain the difference between setting a boolean **with** vs **without** `-P`, then set one boolean the correct way so it **survives a reboot**.

---
*Ask "D9.7 答案" when done, or say "继续" for the next batch.*
