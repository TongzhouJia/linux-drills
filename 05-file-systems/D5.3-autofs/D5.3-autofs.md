# KP D5.3 — autofs
**7 tasks · RHEL 10 · ⭐⭐⭐ server1 · NFS server = 192.168.55.47**

**1.** Install `autofs` and enable+start the service so it survives a reboot.

**2.** Configure autofs with a **wildcard** map so that `/mnt/autofs_home/<user>` automatically mounts each user's home directory from `192.168.55.47:/export/home/<user>`.

**3.** Verify a specific user's directory auto-mounts on access (e.g. `cd /mnt/autofs_home/manny`).

**4.** Configure an **indirect** map so `/nfsdata/share` auto-mounts `192.168.55.47:/export/dba_files`.

**5.** Configure a **direct** map that auto-mounts a share at the absolute path `/mnt/it_files`.

**6.** Ensure the autofs configuration survives a reboot.

**7.** Show that nothing is mounted until the path is accessed, and the mount appears after access; save your evidence (before/after `mount | grep autofs`) to `/root/autofs_check.txt`.

---
*Ask "D5.3 答案" when done, or say "继续" for the next batch.*
