# KP D5.6 — Diagnose & Correct File Permission Problems
**6 tasks · RHEL 10 · server1 as root**

**1.** Users in group `dba_staff` cannot write to `/shared/reports` although they should. Inspect and fix so the owning group has write access.

**2.** The script `/root/bin/run.sh` fails with "permission denied" when executed. Diagnose and fix it.

**3.** A user cannot `cd` into a directory they own. Diagnose and restore the missing bit on the directory.

**4.** A file `/data/report.txt` is owned by `root:root` but should be owned by `manny:dba_staff`. Correct the owner and group.

**5.** New files created in a shared directory keep getting the wrong group. Fix it so new files always get the correct group, and reset the group on the existing files.

**6.** Directory `/data` is mode `700` owned by `root`, but group `it_staff` must be able to read and list it — **without** granting any access to others. Adjust ownership/permissions appropriately.

---
*Ask "D5.6 答案" when done, or say "继续" for the next batch.*
