# KP D7.2 — Hostname Resolution
**5 tasks · RHEL 10 · server1 as root**

**1.** Set server1's hostname to **`lab-server1`** persistently.

**2.** Add static entries to `/etc/hosts` mapping `192.168.55.71` → `lab-server1` and `192.168.55.72` → `lab-server2`.

**3.** Verify name resolution works (ping `lab-server2` by name, and `getent hosts lab-server2`).

**4.** Show the current hostname in all its forms (static / pretty / transient) and save to `/root/hostname.txt`.

**5.** Configure the DNS server used for resolution via `nmcli` (`ipv4.dns`) and verify it appears in `/etc/resolv.conf`.

---
*Ask "D7.2 answers" when done, or say "more" for the next batch.*
