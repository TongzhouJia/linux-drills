# KP D8.3 — Groups & Group Membership
**5 tasks · RHEL 10 · server1 as root**

**1.** Create the groups `dba_admin`, `dba_managers`, `dba_staff`, `dba_intern`, `it_staff`, `it_managers`.

**2.** Add user `manny` to the supplementary groups `dba_admin`, `dba_managers`, and `dba_staff` **without** removing any existing memberships.

**3.** Change the **primary group** of user `jan` to `dba_staff`.

**4.** Create a group named `platforms` with a **specific GID of 5000**.

**5.** Remove user `manny` from the `dba_managers` supplementary group, leaving his other groups intact.

---
*Ask "D8.3 answers" when done, or say "more" for the next batch.*
