# KP D8.1 — User Accounts: create / delete / modify
**6 tasks · RHEL 10 · server1 as root**

**1.** Create a user `manny` with **UID 1010** and the comment `DBA Admin`.

**2.** Create a user `jack` whose login shell is `/sbin/nologin` and who has **no home directory**.

**3.** Modify user `moe` to change the login shell to `/bin/bash` and set the comment to `DBA Staff`.

**4.** Delete user `cindy` **along with her home directory**.

**5.** Create a user `jan` with home directory `/data/jan` and primary group `dba_staff` (assume the group exists).

**6.** Set the account so that `manny`'s login is disabled/locked, then unlock it again.

---
*Ask "D8.1 答案" when done, or say "继续" for the next batch.*
