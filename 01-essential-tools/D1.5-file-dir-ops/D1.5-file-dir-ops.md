# KP D1.5 — File & Directory Operations (cp / mv / rm / mkdir)
**5 tasks · RHEL 10 · server1 as root**

**1.** Create the nested directory tree `/root/projects/2026/q1` in a **single** command.

**2.** Copy `/etc/hosts` and `/etc/hostname` into `/root/projects/`, **preserving** their original permissions and timestamps.

**3.** Recursively copy the entire `/etc/skel` directory — **including hidden files** — to `/root/skel_backup/`.

**4.** Rename the directory `/root/projects/2026` to `/root/projects/current`.

**5.** Create five empty files named `file1` through `file5` inside `/root/projects/current` using a **single** command (brace expansion).

---
*Ask "D1.5 answers" when done, or name the next KP.*
